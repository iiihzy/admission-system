package com.admission.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class SimpleJwtConfig {
    // Simple JWT configuration without factory bean issues
}